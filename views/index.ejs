<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home Page</title>
</head>
<body>
  <!-- Include the navigation bar partial at the top of every page -->
  <%- include('./partials/_navbar.ejs') %>

  <% if (user) { %>
    <h1>Welcome to the app, <%= user.username %>!</h1>
  <% } else { %>
    <h1>Welcome to the app, guest.</h1>
  <% } %>

  <p>Your personal pantry manager and cooking community!</p>
</body>
</html>